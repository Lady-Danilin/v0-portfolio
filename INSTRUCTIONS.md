/* para agregar cosas y luego guardarlas
git add .   --esto guarda absoultamente todos los archivos que no estan nombrados en .gitignore


luego se hace el commit con

git commit -m "mensaje que marque los cambios a commitear"


git push      --si esto no funciona te va a aparecer un mensaje que yo lo copio y lo pego que dice upstream origin/main o algo similar y de ahi en adelante git push funciona siempre